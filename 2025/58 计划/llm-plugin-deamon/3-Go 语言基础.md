
# 3.1 Go 语言介绍
---

Go 语言在多核并发上拥有原生的设计优势，充分利用现代硬件性能又兼容开发效率，设计的目标是为了既发挥类似 python 等动态语言的开发效率，又可以在这样的基础上提供 c 等编译型语言的性能和安全。

Go 语言具备很强的语言表达能力，支持静态类型安全，能够快速编译大型项目；同时也允许开发人员访问底层系统，极力挖掘计算机 CPU 资源，还提供了强大的网络编程和并发编程支持。

Go 语言具备以下特性：

- 从底层支持协程并发，无需第三方库支持，对开发者的编程技巧和开发经验要求低
- 支持自动垃圾回收，避免内存泄漏
- 支持函数多返回值
- 支持匿名函数和闭包
- 支持反射
- 提供强大的标准库支持
- 快速，静态类型的编译型语言，同时提供动态语言特性

# 3.2 环境安装
---


# 3.3 基本语法
---
对于任何一门编程语言，基础和语法是首先要掌握的。Go 语言在语法上和 C 语言有很多相似之处，但是比 C 语言更加简约。

## 3.3.1 变量的声明和初始化
---

## 3.3.2 原生数据类型
---

## 3.3.3 指针
---
在 c/cpp 语言中指针直接操作内存的特性使得 cpp 具备极高的性能，开发人员通过它直接操作和管理大块内存数据。单与此同时，指针偏移，运算和内存释放可能引发的错误也让指针编程饱受诟病。

Go 语言限制了指针类型的偏移和计算能力，使得指针类型具备了指针高效访问的特性，但又不会发生指针偏移，避免了非法修改敏感数据的问题。同时 Go 语言中提供的自动垃圾回收机制，也减少了指针占用内存回收的复杂性。

在 Go 语言中，指针包含以下三个概念：

- 指针地址
- 指针类型
- 指针取值

