## 需求分析
---

## 产品原型
---

![[Pasted image 20250509150736.png]]

在工作流编辑页面，我们的右上角可以看到预览按钮，本节的重点就是讨论这个预览按钮。预览的右边是历史记录查看和工作流潜在问题的列表。左边是环境变量和会话变量设置。

发布的左边是特征设置，这里可以设置工作流从一些特征，比如推荐回答之类的东西。

预览是一个小型的贴在右边的对话框：

![[image.png|627x668]]


## 系统设计
---

### 接口设计
---

### 后端实现
---

controller 层只需要调用 AppgenerateService 的 generate 方法即可，返回一个生成器或者字典（这取决于是流式还是非流式输出），当然这样的返回不可以直接返回给前端，使用一个库工具函数将生成器或者字典构造成可以返回的格式（sse ，字典应该不咋要处理）。

也就是说这一层需要实现两个gon






























