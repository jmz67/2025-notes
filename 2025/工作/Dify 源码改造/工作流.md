
### 发布

![[Pasted image 20250226152319.png]]

publish 按钮，实际上就是更新工作流，对应的接口如下：

```
POST 
http://47.99.172.64:23016/console/api/apps/273a8d6f-8662-44ce-8715-922548ac7428/workflows/publish

{"result": "success", "created_at": 1740554301}
```

在 `app/(commonLayout)/(appDetailLayout)/[appId]/workflow/page.tsx` 中：

```tsx
'use client'

import Workflow from '@/app/components/workflow'

const Page = () => {
  return (
    <div className='w-full h-full overflow-x-auto'>
      <Workflow />
    </div>
  )
}
export default Page
```

在组件 `app/components/workflow/index.tsx` 中定义了 workflow 相关的组件：

```tsx

```

上述的那一排按钮位于 `web/app/components/workflow/header/index.js` 中：

```tsx

```