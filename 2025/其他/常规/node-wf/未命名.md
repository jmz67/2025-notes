
```
npx create-next-app@14.2.5
```


```

```

如何在前端显示当前用户的信息（姓名什么的，头像什么的）在导航栏中，这些信息存在前端哪里？我的理解是和 cookie 存在一起？

用户在当前租户下面的权限也要存起来吗？实际上还是上面这个问题？这些特征要存在哪里？然后要随着 cookie 一起过期吗？还是只保留 cookie 然后跟着经常性地带着 token 请求后端，然后得到权限的信息，这样可以减轻前端的负担？

token 存在哪里，存在哪里有什么区别？

